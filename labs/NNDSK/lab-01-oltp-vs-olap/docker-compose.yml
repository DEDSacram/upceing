services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nndsk
      POSTGRES_USER: nndsk
      POSTGRES_PASSWORD: nndsk
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
